-- always_views.sql
-- This file is always loaded on startup for views and similar objects.
-- Place your CREATE OR REPLACE VIEW statements here.

-- Example:
-- CREATE OR REPLACE VIEW user_metrics_summary AS
-- SELECT u.id AS user_id, u.username, COUNT(m.id) AS metric_count
-- FROM users u
-- LEFT JOIN metric_entries m ON m.user_id = u.id
-- GROUP BY u.id, u.username;
